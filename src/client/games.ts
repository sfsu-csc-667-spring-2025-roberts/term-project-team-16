/*import ui

import getgameid
import socketevents

const elements = {
    START_GAME_BUTTON:
        document.querySelector<HTMLButtonElement>("#start-game-button"),
    OVERLAY: document.querySelector<HTMLDivElement>("#overlay"),
    PLAY_AREA: document.querySelector<HTMLDivElement>("#play-area"),
    CHAT_FORM: document.querySelector<HTMLFormElement>("#chat-form"),
    CHAT_INPUT: document.querySelector<HTMLInputElement>("#chat-input"),
    CHAT_MESSAGES: document.querySelector<HTMLDivElement>("#chat-messages"),
}

//rerror messsage for error getting game

//error for messages


export const configureSocketEvents = (socket: SocketIOClient.Socket) => {
    Object.entries(MESSAGE_MAP).forEach(([event, message]) => {

*/